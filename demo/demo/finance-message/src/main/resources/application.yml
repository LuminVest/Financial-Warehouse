server:
  port: 8120 # 服务端口

spring:
  profiles:
    active: dev # 环境设置
  application:
    name: service-sms # 服务名

  #spring:
  redis:
    host: localhost
    port: 6379
    database: 0
    password:  #默认为空
    timeout: 3000ms #最大等待时间，超时则抛出异常，否则请求一直等待
    lettuce:
      pool:
        max-active: 20  #最大连接数，负值表示没有限制，默认8
        max-wait: -1    #最大阻塞等待时间，负值表示没限制，默认-1
        max-idle: 8     #最大空闲连接，默认8
        min-idle: 0     #最小空闲连接，默认0

  cloud:
    nacos:
      discovery:
        server-addr: localhost:8848 # nacos服务地址
        #ip: 192.168.65.29

  #spring:
#  rabbitmq:
#    host: 192.168.65.137
#    port: 5672
#    virtual-host: /
#    username: guest
#    password: guest


#feign:
#  client:
#    config:
#      default:
#        connectTimeout: 10000 #连接超时配置
#        readTimeout: 600000 #执行超时配置
#  #开启Feign对Sentinel的支持
#  #feign:
#  sentinel:
#    enabled: true

logging:
  level:
    com.tuling.ak.sms.client.CoreUserInfoClient: DEBUG #以什么级别监控哪个接口

SMSUtil:
  serverIp: "app.cloopen.com"
  serverPort: "8883"
  accountSId: "2c94811c946f6bfb0195f54186194232"
  accountToken: "c16c4ef7675c463f8a66cc1d4056e3b6"
  appId: "2c94811c946f6bfb0195f54187b94239"
  templateId: 1


sms:
  serverIp: "app.cloopen.com"
